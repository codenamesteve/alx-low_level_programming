#include <stdio.h>
/**
 * main - Print number arguments(i.e argc)
 * @argc: int number of arguments
 * @argv: char array of strings(argumants)
 * Return: Always 0
*/
int main(int argc, char __attribute__((__unused__)) *argv[])
{
printf("%d\n", argc);
return (0);
}
